name: example
description: Demonstrates how to use the fl_mlkit_translate_text plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_mlkit_translate_text:
    path: ../
  cupertino_icons: ^1.0.4
  flutter_curiosity: ^3.3.2
  flutter_waya: ^1.15.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4


flutter:
  uses-material-design: true
